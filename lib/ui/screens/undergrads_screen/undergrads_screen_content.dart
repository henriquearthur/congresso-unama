import 'package:flutter/material.dart';

class UndergradsScreenContent extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Undergrads Screen"),
      ),
    );
  }
}
